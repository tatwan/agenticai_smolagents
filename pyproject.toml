[project]
name = "smolagents-course"
version = "0.2.0"
description = "Building AI Agents with smolagents — a short, self-guided lab sequence"
requires-python = ">=3.10"
dependencies = [
    "smolagents[toolkit,litellm]>=1.26,<2",
    "huggingface-hub>=0.34.0",
    "mlflow>=2.20,<4",
    "jupyter>=1.0.0",
    "ipykernel>=6.0.0",
    "python-dotenv>=1.0.0",
    "pandas>=2.0.0",
    "requests>=2.31.0",
    "markdownify>=0.13.0",
]

[tool.uv]
package = false
